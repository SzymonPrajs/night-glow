# `nightglow-astronomy`

The implemented first slice resolves one pinned UTC instant against explicit
Earth-orientation and leap-second data identities. It rejects ambient `latest`
data so scenario results remain reproducible.

This is the ownership foothold for time scales, frames, ephemerides, apparent
place, stellar motion, and sky indexing. None of those production calculations
is claimed by the fixture resolver.
