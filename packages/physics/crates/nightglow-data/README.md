# `nightglow-data`

This crate decodes Physics-owned data/terrain manifests and exposes the
independent conforming adapter for language-neutral Environment releases. It
validates dependency identities but owns no provider fetch, filesystem, cache,
environmental reconstruction, or physical equation.
