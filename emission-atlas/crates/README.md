# Planned Rust crates

No crates exist during the design phase. The proposed boundaries and gated creation order are in [the implementation roadmap](../docs/12-implementation-roadmap.md). This file prevents an empty directory from disappearing without implying implementation.
